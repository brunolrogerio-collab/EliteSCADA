# L3 acceptance validation trigger

This documentation-only marker intentionally triggers the dedicated L3 workflow on the documentation-inclusive branch HEAD so issue #180 can obtain dedicated L3 and normal EliteSCADA CI evidence on one exact SHA.

It does not alter test filters, assertions, runtime behavior, protocol fixtures, or acceptance criteria.
